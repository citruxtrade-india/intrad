# ============================================================
# Anti-Gravity Trading Platform - Production Requirements
# Python 3.7 compatible (tested locally)
# Install: pip install -r requirements.txt
# Note: pya3 must be installed separately (see below)
# ============================================================

# Web framework
fastapi==0.103.2
uvicorn==0.22.0
starlette==0.27.0

# Data validation
pydantic==2.5.3
pydantic-core==2.14.6
annotated-types==0.5.0

# Auth & environment
python-dotenv==0.21.1
pyotp==2.9.0

# HTTP & networking
requests==2.31.0
urllib3==1.26.20
certifi==2026.1.4
charset-normalizer==3.4.4
idna==3.10

# WebSocket
websocket-client==1.6.1
rel==0.4.9.23

# Data processing
numpy==1.21.6
pandas==1.1.5
python-dateutil==2.9.0.post0
pytz==2025.2
six==1.17.0

# Async
anyio==3.7.1
sniffio==1.3.1
exceptiongroup==1.3.1
h11==0.14.0
click==8.1.8
colorama==0.4.6
importlib-metadata==6.7.0
typing-extensions==4.7.1
zipp==3.15.0

# ============================================================
# MANUAL INSTALL (after above):
#   pip install pya3==1.0.30 --no-deps
#
# DO NOT install google-generativeai (incompatible with Py3.7)
# The GuidanceAgent has a safe fallback if it's missing.
# ============================================================
